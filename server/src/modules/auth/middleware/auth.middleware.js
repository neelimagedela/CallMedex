const authenticate = async (req, res, next) => {
  // TODO:
  // 1. Read access token
  // 2. Verify token
  // 3. Attach authenticated user
  // 4. Continue request
};

module.exports = {
  authenticate,
};
